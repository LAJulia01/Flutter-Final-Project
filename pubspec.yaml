name: module2_4_lab_exercise
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.7.0
  flutter_polyline_points: ^2.1.0
  flutter_map: ^7.0.2
  google_maps_flutter: ^2.2.5
  geolocator: ^13.0.1
  foundation: ^0.0.5
  logger: ^2.4.0
  location: ^7.0.1
  lib: ^0.0.3
  floating_navigation_bar: ^0.0.5
  file_picker: ^8.1.3
  open_file: ^3.5.9
  path_provider: ^2.1.4
  image_picker: ^1.1.2
  firebase_core: ^3.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
